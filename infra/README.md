# Infrastructure

This directory is reserved for non-Docker infrastructure assets.

Docker-specific assets live under `docker/`:
- `docker/docker-compose.yml`
- `docker/Dockerfile`
- `docker/nginx/default.conf`
